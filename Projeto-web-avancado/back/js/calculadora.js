// USANDO ARROWS FUNC
const somar = (a, b) => a + b;

const subtrair = (a, b) => a - b;

const multiplicar = (a, b) => a * b;

const dividir = (a, b) => a / b;

// Criando o export
module.exports =
{
    somar,
    dividir,
    subtrair,
    multiplicar
}